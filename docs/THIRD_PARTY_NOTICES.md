# Third-party notices

- Windows App SDK / WinUI 3: Microsoft license terms apply.
- OpenCvSharp4 and OpenCV: Apache-2.0/BSD-style upstream licenses; see the NuGet package contents and upstream repositories.
- FFmpeg: the GitHub Actions workflow downloads the Gyan Windows essentials build. Its exact license/configuration is printed by `ffmpeg -version`; the artifact distributor must retain the accompanying FFmpeg license obligations and provide corresponding source where required.
- xUnit, Microsoft.NET.Test.Sdk and coverlet are build/test dependencies only.

CleanFrame Video 2 does not include LaMa, an ONNX model, DirectML, FFmpeg `delogo`, SynthID handling, invisible watermark removal or Content Credentials removal.
