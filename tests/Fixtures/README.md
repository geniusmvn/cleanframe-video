# Regression source fixtures missing

The handoff ZIP names these source videos but does not contain them:

- `haizz(1).mp4`
- `Wool_puppets_sleeping_in_moonlight_202607071922.mp4`

Do not reconstruct or infer them from the 3-second preview outputs. Place legally available originals here only when the owner supplies them, then replace the skipped regression test with a real end-to-end assertion.
