<Application
    x:Class="Erasa.Video2.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <Color x:Key="ErasaOrange">#FFFF6400</Color>
        <Color x:Key="ErasaOrangeHover">#FFE95800</Color>
        <Color x:Key="ErasaInk">#FF17191D</Color>
        <Color x:Key="ErasaMuted">#FF6B6F76</Color>
        <Color x:Key="ErasaBorder">#FFE6E2DD</Color>
        <Color x:Key="ErasaSurface">#FFFBFAF8</Color>
        <SolidColorBrush x:Key="ErasaOrangeBrush" Color="{StaticResource ErasaOrange}" />
        <SolidColorBrush x:Key="ErasaInkBrush" Color="{StaticResource ErasaInk}" />
        <SolidColorBrush x:Key="ErasaMutedBrush" Color="{StaticResource ErasaMuted}" />
        <SolidColorBrush x:Key="ErasaBorderBrush" Color="{StaticResource ErasaBorder}" />
        <SolidColorBrush x:Key="ErasaSurfaceBrush" Color="{StaticResource ErasaSurface}" />

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ErasaOrangeBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFF5F2EE" />
            <Setter Property="Foreground" Value="{StaticResource ErasaInkBrush}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="BorderBrush" Value="{StaticResource ErasaBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="ToolToggleStyle" TargetType="ToggleButton">
            <Setter Property="MinWidth" Value="58" />
            <Setter Property="Height" Value="38" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="BorderBrush" Value="{StaticResource ErasaBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Application.Resources>
</Application>
