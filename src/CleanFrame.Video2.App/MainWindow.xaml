<Window
    x:Class="CleanFrame.Video2.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CleanFrame.Video2.App.Controls"
    Title="CleanFrame Video 2">
    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,14" BorderThickness="0,0,0,1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <Border Width="38" Height="38" CornerRadius="10" Background="{ThemeResource AccentFillColorDefaultBrush}">
                        <FontIcon Glyph="&#xE91B;" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock Text="CleanFrame Video 2" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock Text="Temporal reconstruction • user-confirmed mask" Opacity="0.65" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button Content="Thêm file" Click="AddFiles_Click" />
                    <Button Content="Thêm thư mục" Click="AddFolder_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Padding="16" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="Hàng đợi" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" x:Name="QueueCountText" Text="0 video" Opacity="0.65" VerticalAlignment="Center" />
                    </Grid>
                    <ListView Grid.Row="1" x:Name="QueueList" Margin="0,10,0,10" SelectionChanged="QueueList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="4" ColumnSpacing="10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding StatusText}" Opacity="0.65" FontSize="12" />
                                        <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="3" Margin="0,6,0,0" />
                                    </StackPanel>
                                    <FontIcon Grid.Column="1" Glyph="{Binding StatusGlyph}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <TextBlock Grid.Row="2" Text="Kéo video vào cửa sổ để thêm nhanh." TextWrapping="Wrap" Opacity="0.6" FontSize="12" />
                </Grid>
            </Border>

            <Grid Grid.Column="1" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Padding="14,10" CornerRadius="12" Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="1  Thả video" HorizontalAlignment="Center" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="2  Tự đề xuất" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="3  Chỉnh mask" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="3" Text="4  Preview 3 giây" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="4" Text="5  Xử lý tất cả" HorizontalAlignment="Center" />
                    </Grid>
                </Border>

                <Border Grid.Row="1" CornerRadius="14" Background="#FF090B10">
                    <ScrollViewer x:Name="EditorScroll" ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="5"
                                  HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled"
                                  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid x:Name="VideoStage" Width="960" Height="540">
                            <MediaPlayerElement x:Name="Player" AreTransportControlsEnabled="True" Stretch="Uniform" />
                            <controls:MaskEditorControl x:Name="MaskEditor" />
                            <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="14" Padding="10,6" CornerRadius="8" Background="#B0000000">
                                <TextBlock x:Name="SelectedFileText" Text="Chưa chọn video" Foreground="White" TextTrimming="CharacterEllipsis" MaxWidth="500" />
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </Border>

                <Border Grid.Row="2" Padding="10" CornerRadius="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="20"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="160"/><ColumnDefinition Width="20"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ToggleButton x:Name="RectangleTool" Content="Khung" IsChecked="True" Click="Tool_Click" Tag="Rectangle" />
                        <ToggleButton Grid.Column="1" x:Name="EllipseTool" Content="Elip" Click="Tool_Click" Tag="Ellipse" />
                        <ToggleButton Grid.Column="2" x:Name="BrushTool" Content="Cọ" Click="Tool_Click" Tag="Brush" />
                        <ToggleButton Grid.Column="3" x:Name="EraserTool" Content="Tẩy" Click="Tool_Click" Tag="Eraser" />
                        <ToggleButton Grid.Column="4" x:Name="PanTool" Content="Pan" Click="Tool_Click" Tag="Pan" />
                        <TextBlock Grid.Column="6" Text="Cỡ cọ" VerticalAlignment="Center" />
                        <Slider Grid.Column="7" x:Name="BrushSizeSlider" Minimum="0.005" Maximum="0.08" Value="0.025" ValueChanged="BrushSize_Changed" />
                        <TextBlock Grid.Column="9" Text="Mask mềm" VerticalAlignment="Center" />
                        <Slider Grid.Column="10" x:Name="SoftnessSlider" Minimum="0" Maximum="1" Value="0.45" ValueChanged="Softness_Changed" />
                        <Button Grid.Column="12" Content="Hoàn tác" Click="Undo_Click" />
                        <Button Grid.Column="13" Content="Làm lại" Click="Redo_Click" />
                        <Button Grid.Column="14" Content="Đặt lại" Click="ResetMask_Click" />
                    </Grid>
                </Border>

                <Grid Grid.Row="3" ColumnSpacing="10">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <Button Content="Tự đề xuất" Click="Suggest_Click" />
                    <Button Grid.Column="1" Content="Preview 3 giây" Click="Preview_Click" />
                    <ComboBox Grid.Column="2" x:Name="ModeCombo" SelectedIndex="1" Width="130">
                        <ComboBoxItem Content="Fast" Tag="Fast" />
                        <ComboBoxItem Content="Beautiful" Tag="Beautiful" />
                    </ComboBox>
                    <CheckBox Grid.Column="3" x:Name="ApplySameRatioCheck" Content="Áp mask cho video cùng tỉ lệ" IsChecked="True" VerticalAlignment="Center" />
                    <Button Grid.Column="5" Content="Xử lý tất cả" Style="{StaticResource AccentButtonStyle}" Click="ProcessAll_Click" />
                </Grid>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Padding="16,10" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <ProgressRing x:Name="BusyRing" Width="18" Height="18" IsActive="False" />
                    <TextBlock x:Name="StatusText" Text="Sẵn sàng" VerticalAlignment="Center" />
                </StackPanel>
                <Button Grid.Column="1" Content="Tạm dừng" Click="Pause_Click" />
                <Button Grid.Column="2" Content="Tiếp tục" Click="Resume_Click" />
                <Button Grid.Column="3" Content="Huỷ" Click="Cancel_Click" />
                <Button Grid.Column="4" Content="Thử lại" Click="Retry_Click" />
            </Grid>
        </Border>

        <InfoBar x:Name="ErrorBar" Grid.Row="0" Grid.RowSpan="3" VerticalAlignment="Top" Margin="20,80,20,0"
                 IsOpen="False" Severity="Error" Title="Worker gặp lỗi" IsClosable="True" />
    </Grid>
</Window>
